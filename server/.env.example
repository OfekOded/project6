# File: server/.env.example
# Purpose: template for the real .env (copy to .env and fill in). .env is git-ignored.
# Owner: Partner A | shared infra - FROZEN
# Stage: B (שלב ב)
DB_HOST=localhost
DB_PORT=3306
DB_USER=app_server
DB_PASSWORD=change_me
DB_NAME=fullstack6
PORT=3000
