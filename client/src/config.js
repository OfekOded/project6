/**
 * File: client/src/config.js
 * Purpose: single place for the server base URL (used by every fetch in the app).
 * Owner: Partner B | SHARED INFRA - FROZEN after kickoff
 * Stage: C (שלב ג)
 */
export const API_URL = 'http://localhost:3000';
